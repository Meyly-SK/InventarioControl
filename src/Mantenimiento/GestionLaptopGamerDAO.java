package Mantenimiento;

import java.sql.Connection;
import java.sql.PreparedStatement;
import java.sql.ResultSet;
import java.sql.SQLException;
import java.util.ArrayList;

import Entidad.LaptopG;
import Entidad.PCGamer;
import Interfaces.LaptopGamerInterfaceDAO;
import Utils.MySQLConexion8;

public class GestionLaptopGamerDAO implements LaptopGamerInterfaceDAO{

	@Override
	public int registrarLaptopGamer(LaptopG a) {
		int res = 0;
		PreparedStatement pstn = null;
		Connection con = null;

		try {
			// 1. Conexion a la bd
			con = MySQLConexion8.getConexion();
			// 2. Establecer la instruccion SQL
			String sql = "insert tb_laptopgamer values (null,?,?,?,?,?,?,?,?,?)";
			// 3. Preparar la instruccion --> obteniendo comandos SQL
			pstn = con.prepareStatement(sql);

			// 4. Parametros
			pstn.setString(1, a.getCodigo());
			pstn.setString(2, a.getMarca());
			pstn.setString(3, a.getModelo());
			pstn.setString(4, a.getProcesador());
			pstn.setString(5, a.getTarjVideo());
			pstn.setString(6, a.getSize());
			pstn.setDouble(7, a.getPrecio());
			pstn.setInt(8, a.getStock());
			pstn.setString(9, a.getCategoria());

			// 5. Ejecutar la instruccion
			res = pstn.executeUpdate();

		} catch (Exception ex) {
			System.out.println("Error en la instruccion SQL REGISTRAR LAPTOP GAMER " + ex.getMessage());
		} finally {
			try {
				if (con != null)
					con.close();
				if (pstn != null)
					pstn.close();
			} catch (SQLException e2) {

			}
		}

		return res;
	}

	@Override
	public int EliminarLaptopGamer(LaptopG a) {
		int res = 0;
		PreparedStatement pstn = null;
		Connection con = null;

		try {
			// 1. Conexion a la bd
			con = MySQLConexion8.getConexion();
			// 2. Establecer la instruccion SQL
			String sql = "delete from tb_laptopgamer where codigo = ?";
			// 3. Preparar la instruccion --> obteniendo comandos SQL
			pstn = con.prepareStatement(sql);

			// 4. Parametros
			pstn.setString(1, a.getCodigo());

			// 5. Ejecutar la instruccion
			res = pstn.executeUpdate();

		} catch (Exception ex) {
			System.out.println("Error en la instruccion SQL ELIMINAR LAPTOP GAMER " + ex.getMessage());
		} finally {
			try {
				if (con != null)
					con.close();
				if (pstn != null)
					pstn.close();
			} catch (SQLException e2) {
				// TODO: handle exception
			}
		}

		return res;
	}

	@Override
	public int actualizarLaptopGamer(LaptopG a) {
		int res = 0;
		PreparedStatement pstn = null;
		Connection con = null;

		try {
			// 1. Conexion a la bd
			con = MySQLConexion8.getConexion();
			// 2. Establecer la instruccion SQL
			String sql = "{call ActualizarLaptopGamer(?,?,?,?,?,?,?,?)};";
			// 3. Preparar la instruccion --> obteniendo comandos SQL
			pstn = con.prepareStatement(sql);

			// 4. Parametros
			pstn.setString(1, a.getMarca());
			pstn.setString(2, a.getModelo());
			pstn.setString(3, a.getProcesador());
			pstn.setString(4, a.getTarjVideo());
			pstn.setString(5, a.getSize());
			pstn.setDouble(6, a.getPrecio());
			pstn.setInt(7, a.getStock());
			pstn.setString(8, a.getCodigo());


			// 5. Ejecutar la instruccion
			res = pstn.executeUpdate();

		} catch (Exception ex) {
			System.out.println("Error en la instruccion SQL ACTUALIZAR LAPTOP GAMER " + ex.getMessage());
		} finally {
			try {
				if (con != null)
					con.close();
				if (pstn != null)
					pstn.close();
			} catch (SQLException e2) {
				// TODO: handle exception
			}
		}

		return res;
	}

	@Override
	public ArrayList<Object> leerColumnas() {
		ArrayList<Object> lista = new ArrayList<Object>();

		PreparedStatement pstm = null;
		Connection con = null;
		ResultSet res = null;

		try {
			// Paso 1: Establecer la conexion con la base de datos
			con = MySQLConexion8.getConexion();

			// PASO 2: establecer la instruccion SQL registrar
			String sql = "SHOW COLUMNS FROM bd_upmorbsluck.tb_laptopgamer;";

			// Paso 3: Enviar la variable SQL al objeto (pstm) ---> obtener los comandos
			pstm = con.prepareStatement(sql);

			// PAso 4: Parametros -------> No hay

			// Paso 5: ejecutar la instruccion
			res = pstm.executeQuery();

			// paso 6

			while (res.next()) {

				lista.add(res.getString(1));

			}

		} catch (Exception e) {
			System.out.println("Error >>> En la instruccion SQL LEER COLUMNAS LAPTOP GAMER " + e.getMessage());
		} finally {
			try {
				if (pstm != null) {
					pstm.close();
				}

				if (con != null) {
					con.close();
				}

				if (res != null) {
					res.close();
				}
			} catch (Exception e2) {
				System.out.println("Error al cerrar la base de datos--->" + e2.getMessage());
			}
		}
		lista.remove(lista.indexOf("codigo_"));
		lista.remove(lista.indexOf("categoria"));
		return lista;
	}

	@Override
	public ArrayList<LaptopG> buscarLaptopGamer(String name) {
		// TODO Auto-generated method stub
		return null;
	}

	@Override
	public ArrayList<LaptopG> leerLaptopGamer() {

		ArrayList<LaptopG> lista = new ArrayList<LaptopG>();
		LaptopG lGamer;

		PreparedStatement pstm = null;
		Connection con = null;
		ResultSet res = null;

		try {
			// Paso 1: Establecer la conexion con la base de datos
			con = MySQLConexion8.getConexion();

			// PASO 2: establecer la instruccion SQL listar consolas
			String sql = "{call listarLaptopGamer()};";

			// Paso 3: Enviar la variable SQL al objeto (pstm) ---> obtener los comandos
			pstm = con.prepareStatement(sql);

			// PAso 4: Parametros -------> No hay

			// Paso 5: ejecutar la instruccion
			res = pstm.executeQuery();

			// paso 6

			while (res.next()) {
				lGamer = new LaptopG();
				lGamer.setCodigo_(res.getInt(1));
				lGamer.setCodigo(res.getString(2));
				lGamer.setMarca(res.getString(3));
				lGamer.setModelo(res.getString(4));
				lGamer.setProcesador(res.getString(5));
				lGamer.setTarjVideo(res.getString(6));
				lGamer.setSize(res.getString(7));
				lGamer.setPrecio(res.getDouble(8));
				lGamer.setStock(res.getInt(9));
				lGamer.setCategoria(res.getString(10));

				lista.add(lGamer);
			}

		} catch (Exception e) {
			System.out.println("Error >>> En la instruccion SQL LEER LAPTOP GAMER " + e.getMessage());
		} finally {
			try {
				if (pstm != null) {
					pstm.close();
				}

				if (con != null) {
					con.close();
				}

				if (res != null) {
					res.close();
				}
			} catch (Exception e2) {
				System.out.println("Error al cerrar la base de datos--->" + e2.getMessage());
			}
		}
		return lista;

	}

}
